package Utilities;

import testCases.TestBase;


public class UsefullFunctions extends TestBase {
	
	
	
	//To click on the End key from the Lab keyboard to scroll down to the end of the page
	//Actions action = new Actions(driver);
	//actions.sendKeys(Keys.END).perform();
	
	//To wait be the old way by Java way it will wait for 3 seconds
//	Thread.sleep(3000);
	
	
	//To Concatenation to element text in one string and assert if this name was the changed name or not
//	String fullName = MyAccountPageObject.firstNameProfileTXB.getText() + MyAccountPageObject.lastNameProfileTXB.getText();
//	assertTrue(MyAccountPageObject.userFullName.getText().contains(fullName));

	

}
